server:
  addr: ":8080"
postgres:
  dsn: "host=127.0.0.1 port=5432 user=vf password=vf dbname=video_factory sslmode=disable"
jwt:
  secret: "dev-secret-change-me"
  ttl_hour: 168
agent:
  base_url: "http://localhost:8090"
  timeout: 30
